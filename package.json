{
	"name": "root",
	"version": "0.0.0",
	"private": true,
	"homepage": "https://astro-aws.lshay.dev/",
	"repository": {
		"type": "git",
		"url": "ssh://git@github.com/lukeshay/astro-aws.git",
		"directory": "."
	},
	"license": "MIT",
	"workspaces": [
		"packages/*",
		"apps/*",
		"scripts"
	],
	"scripts": {
		"lint": "eslint .",
		"lint:fix": "yarn lint --fix",
		"prettier": "prettier --check .",
		"prettier:fix": "yarn prettier --write",
		"release:cut": "git commit -am 'new release' && yarn changeset publish && git push && git push --tags",
		"release:prepare": "yarn build -r && yarn changeset version && yarn prettier:fix"
	},
	"prettier": "@lshay/prettier-config",
	"eslintConfig": {
		"extends": [
			"../../.eslintrc.cjs"
		]
	},
	"dependencies": {
		"@changesets/changelog-github": "^0.4.7",
		"@changesets/cli": "^2.25.2",
		"@lshay/eslint-config": "^0.2.1",
		"@lshay/prettier-config": "^0.5.2",
		"@rushstack/eslint-patch": "^1.2.0",
		"eslint": "^8.26.0",
		"eslint-import-resolver-typescript": "^3.5.2",
		"eslint-plugin-import": "^2.26.0",
		"prettier": "^2.7.1",
		"typescript": "^4.8.4"
	},
	"packageManager": "yarn@3.2.4",
	"engines": {
		"node": "14.x || 16.x"
	}
}
